{
  "name": "aura-protocol-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Aura Protocol ZK-ID system",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network polygon-mumbai",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.17.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.0",
    "dotenv": "^16.6.1"
  }
}
